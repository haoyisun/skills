# 如何快速看懂一篇技术内容

1. 安装 `read-fast` skill。
2. 调用 `/read-fast <地址>`。
3. 提供网页 URL、本地文件、PDF，或直接粘贴正文。
4. 让 skill 只保留主线、核心概念和关键结论。

skill 会产出到 `.reader/quick/`，目标是 2–10 分钟读完。短内容不会硬砍；长内容只保留主线，并指出哪些内容如果还想深读应该交给 `/read-standard`。

